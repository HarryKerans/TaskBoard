# Recorded API fixtures are stored here.
# Run POST /api/fixtures/record while authenticated to capture live Alexa data.
# Set ALEXA_FIXTURE_MODE=true in .env to replay fixtures instead of hitting Amazon.
