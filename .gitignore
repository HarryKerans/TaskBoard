# Dependencies
task_board/frontend/node_modules/

# Frontend production build (generated by Docker, not committed)
task_board/frontend/build/

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python / uv
task_board/backend/.venv/
task_board/backend/**/__pycache__/
task_board/backend/**/*.pyc

# Secrets — use .env.example as a template, never commit the real .env
task_board/backend/.env

# Local dev SQLite database
task_board/backend/tasks.dev.db
*.dev.db
tasks.db

# Reference docs (pyalexatodo clone, not part of the add-on)
task_board/backend/pyalexatodo
