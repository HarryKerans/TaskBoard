name: "taskboard"
description: "A tiny local test app for the task dashboard"
version: "0.1.0"
slug: "taskboard"
init: false

arch:
  - aarch64
  - amd64

startup: application
boot: manual

ports:
  8000/tcp: 8099

options:
  database_path: "/data/tasks.db"

schema:
  database_path: "str"
