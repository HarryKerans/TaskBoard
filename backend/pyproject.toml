[project]
name = "test-board-backend"
version = "0.1.0"
description = "Alexa To-Do API backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "aiohttp",
    "aioamazondevices",
    "python-dotenv>=1.2.2",
    "aiofiles>=25.1.0",
]

[dependency-groups]
dev = []
